Start Wildfly
sudo systemctl start wildfly

Status Wildfly
sudo systemctl status wildfly

Restart Wildfly
sudo systemctl restart wildfly

Deploy:
mvn clean install wildfly:deploy -Pwildfly-runtime


Tasks:

Not working:
-Email working but sends 2 at a time
-Two users same Email crashes
-Create item with no file selected -> CRASH


Not Done:
-Report + ER
-Shortcuts to all Web


Test:
-Logging(Done but need to know where to check)
